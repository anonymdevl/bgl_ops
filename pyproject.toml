[project]
name = "bgl_ops"
authors = [{ name = "Powersoft", email = "ai4powersoft@gmail.com" }]
description = "BGL Operations - trips & cubic tracking, staff loans & advances, leave visibility, and an executive command center for Betonsa Ghana Limited."
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.bench.dev-dependencies]
